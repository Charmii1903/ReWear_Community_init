const express = require('express');
const router = express.Router();

// Placeholder for future skill management features
router.get('/', (req, res) => {
  res.json({ message: 'Skills route - future feature' });
});

module.exports = router; 